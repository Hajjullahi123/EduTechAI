services:
  - type: web
    name: edutechai
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
